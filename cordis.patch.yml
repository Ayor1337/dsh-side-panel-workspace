# dsh-plugin-side-drawer 的 bundle 补丁层。
# 在 dsh-base / dsh-web-app 两层之后应用。宿主行注册 /drawer/terminal/* HTTP 路由
# （node-pty 终端会话），浏览器半通过 package.json 的 dsh.client 声明被发现并注入。
- insert:
    - id: side-drawer
      name: 'dsh-plugin-side-drawer'
      inject: [webServer]
